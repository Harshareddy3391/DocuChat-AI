services:

  backend:
    build:
      context: ./backend
    container_name: docuchat-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: docuchat-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped